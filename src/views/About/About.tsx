import React from 'react';
import "./about.scss";

interface Props {
}

const About:React.FunctionComponent<Props> = (props) => {
    return (
        <div>about</div>
    )
};

export default About;
